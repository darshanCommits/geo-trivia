{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "target": "es6",
    "module": "esnext", // Or "esnext", depending on your backend
    "outDir": "./dist",
    "composite": true,
    "noEmit": true,
    "removeComments": true

  },
  "include": [ "./*.ts", "./**/*.ts", "../shared/**/*.ts" ], // Add this line
  "exclude": [ "node_modules" ]
}

